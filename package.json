{
  "name": "pocketflare",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "make build",
    "check:pb-version": "node scripts/check-pb-version.mjs",
    "deploy": "make deploy",
    "dev": "wrangler dev"
  }
}
