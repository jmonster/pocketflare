{
  "name": "pocketflare",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "make build",
    "check:pb-version": "node scripts/check-pb-version.mjs",
    "deploy": "make deploy",
    "dev": "wrangler dev",
    "proof:critical": "bash scripts/proof-critical.sh",
    "proof:critical:remote": "bash scripts/proof-critical.sh --remote",
    "test:e2e:restore-ui": "playwright test tests/e2e/restore-start-over.spec.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
