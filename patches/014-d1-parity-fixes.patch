diff --git a/apis/sql.go b/apis/sql.go
index 1db91f9..24fff13 100644
--- a/apis/sql.go
+++ b/apis/sql.go
@@ -207,16 +207,115 @@ func executeQuery(app core.App, query string, maxRows int) (*runSQLResult, error
 	return result, nil
 }
 
-// splitSQLStatements splits a SQL string on ";" delimiters,
-// skipping empty segments and trimming whitespace.
+// splitSQLStatements splits a SQL string on ";" delimiters using a
+// rune-based state machine that respects string literals, blob literals,
+// and comments so that semicolons inside those constructs are not treated
+// as statement boundaries.
 func splitSQLStatements(query string) []string {
-	parts := strings.Split(query, ";")
-	out := make([]string, 0, len(parts))
-	for _, p := range parts {
-		p = strings.TrimSpace(p)
-		if p != "" {
-			out = append(out, p)
+	var out []string
+	runes := []rune(query)
+	var buf strings.Builder
+	n := len(runes)
+
+	const (
+		sNormal = iota
+		sSingleQuote
+		sDoubleQuote
+		sBlob
+		sLineComment
+		sBlockComment
+	)
+
+	st := sNormal
+
+	for i := 0; i < n; i++ {
+		ch := runes[i]
+
+		switch st {
+		case sNormal:
+			switch {
+			case ch == '\'':
+				st = sSingleQuote
+				buf.WriteRune(ch)
+			case ch == '"':
+				st = sDoubleQuote
+				buf.WriteRune(ch)
+			case (ch == 'X' || ch == 'x') && i+1 < n && runes[i+1] == '\'':
+				buf.WriteRune(ch)
+				buf.WriteRune(runes[i+1])
+				i++
+				st = sBlob
+			case ch == '-' && i+1 < n && runes[i+1] == '-':
+				buf.WriteString("--")
+				i++
+				st = sLineComment
+			case ch == '/' && i+1 < n && runes[i+1] == '*':
+				buf.WriteString("/*")
+				i++
+				st = sBlockComment
+			case ch == ';':
+				s := strings.TrimSpace(buf.String())
+				if s != "" {
+					out = append(out, s)
+				}
+				buf.Reset()
+			default:
+				buf.WriteRune(ch)
+			}
+
+		case sSingleQuote:
+			buf.WriteRune(ch)
+			if ch == '\'' {
+				if i+1 < n && runes[i+1] == '\'' {
+					i++
+					buf.WriteRune(runes[i])
+				} else {
+					st = sNormal
+				}
+			}
+
+		case sDoubleQuote:
+			buf.WriteRune(ch)
+			if ch == '"' {
+				if i+1 < n && runes[i+1] == '"' {
+					i++
+					buf.WriteRune(runes[i])
+				} else {
+					st = sNormal
+				}
+			}
+
+		case sBlob:
+			buf.WriteRune(ch)
+			if ch == '\'' {
+				if i+1 < n && runes[i+1] == '\'' {
+					i++
+					buf.WriteRune(runes[i])
+				} else {
+					st = sNormal
+				}
+			}
+
+		case sLineComment:
+			buf.WriteRune(ch)
+			if ch == '\n' {
+				st = sNormal
+			}
+
+		case sBlockComment:
+			buf.WriteRune(ch)
+			if ch == '*' && i+1 < n && runes[i+1] == '/' {
+				i++
+				buf.WriteRune('/')
+				st = sNormal
+			}
 		}
 	}
+
+	// Last statement
+	if s := strings.TrimSpace(buf.String()); s != "" {
+		out = append(out, s)
+	}
+
 	return out
 }
diff --git a/core/app.go b/core/app.go
index e0ad05f..a1dc32f 100644
--- a/core/app.go
+++ b/core/app.go
@@ -61,6 +61,10 @@ type App interface {
 	// (closing db connections, stopping cron ticker, etc.).
 	ResetBootstrapState() error
 
+	// SetSkipSystemMigrations controls whether system schema migrations
+	// are skipped during the next Bootstrap call.
+	SetSkipSystemMigrations(skip bool)
+
 	// DataDir returns the app data directory path.
 	DataDir() string
 
diff --git a/core/base.go b/core/base.go
index 2a5d662..8bb95ae 100644
--- a/core/base.go
+++ b/core/base.go
@@ -71,6 +71,12 @@ type BaseAppConfig struct {
 	SkipSystemMigrations bool
 }
 
+// SetSkipSystemMigrations controls whether system schema migrations are
+// skipped during the next Bootstrap call.
+func (app *BaseApp) SetSkipSystemMigrations(skip bool) {
+	app.config.SkipSystemMigrations = skip
+}
+
 // ensures that the BaseApp implements the App interface.
 var _ App = (*BaseApp)(nil)
 
diff --git a/core/collection_import.go b/core/collection_import.go
index 7119cc4..d48a7f6 100644
--- a/core/collection_import.go
+++ b/core/collection_import.go
@@ -138,7 +138,12 @@ func (app *BaseApp) ImportCollections(toImport []map[string]any, deleteMissing b
 		return res
 	})
 
-	return app.RunInTransaction(func(txApp App) error {
+	// Store the merged collection map so that FindCollectionByNameOrId
+	// and findCollectionsByIdentifiers can resolve during the import.
+	app.Store().Set("pocketflareImportCollections", mappedImported)
+	defer app.Store().Set("pocketflareImportCollections", nil)
+
+		return app.RunInTransaction(func(txApp App) error {
 		existingCollections := []*Collection{}
 		if err := txApp.CollectionQuery().OrderBy("updated ASC").All(&existingCollections); err != nil {
 			return err
diff --git a/core/collection_model.go b/core/collection_model.go
index a8fdedc..8793538 100644
--- a/core/collection_model.go
+++ b/core/collection_model.go
@@ -884,12 +884,19 @@ func onCollectionSaveExecute(e *CollectionEvent) error {
 	var viewFields FieldsList
 	var normalizedViewQuery string
 	if e.Collection.IsView() {
+		// Use the parent app if we are inside a transaction to avoid
+		// creating temp views within a D1 batch where reads after
+		// writes are not allowed.
+		viewApp := e.App
+		if info := e.App.TxInfo(); info != nil && info.parent != nil {
+			viewApp = info.parent
+		}
 		var err error
-		viewFields, err = e.App.CreateViewFields(e.Collection.ViewQuery)
+		viewFields, err = viewApp.CreateViewFields(e.Collection.ViewQuery)
 		if err != nil {
 			return err
 		}
-		normalizedViewQuery, err = normalizeViewQueryId(e.App, e.Collection.ViewQuery)
+		normalizedViewQuery, err = normalizeViewQueryId(viewApp, e.Collection.ViewQuery)
 		if err != nil {
 			return fmt.Errorf("failed to normalize view query id: %w", err)
 		}
diff --git a/core/collection_query.go b/core/collection_query.go
index e84564a..521e931 100644
--- a/core/collection_query.go
+++ b/core/collection_query.go
@@ -60,6 +60,24 @@ func (app *BaseApp) ReloadCachedCollections() error {
 
 // FindCollectionByNameOrId finds a single collection by its name (case insensitive) or id.
 func (app *BaseApp) FindCollectionByNameOrId(nameOrId string) (*Collection, error) {
+	// During an import the merged collection map is stored so that lookups
+	// resolve from the in-memory map instead of querying the database
+	// (which may not reflect in-progress changes on D1).
+	if m, ok := app.Store().GetOk("pocketflareImportCollections"); ok {
+		if imported, ok := m.(map[string]*Collection); ok {
+			// check by id first
+			if c, exists := imported[nameOrId]; exists {
+				return c, nil
+			}
+			// check by name (case insensitive)
+			for _, c := range imported {
+				if strings.EqualFold(c.Name, nameOrId) {
+					return c, nil
+				}
+			}
+		}
+	}
+
 	m := &Collection{}
 
 	err := app.CollectionQuery().
diff --git a/core/collection_record_table_sync.go b/core/collection_record_table_sync.go
index 3ce9255..894572a 100644
--- a/core/collection_record_table_sync.go
+++ b/core/collection_record_table_sync.go
@@ -166,6 +166,21 @@ func normalizeSingleVsMultipleFieldChanges(app App, newCollection *Collection, o
 		return nil // view or not an update
 	}
 
+	// Fetch current view definitions outside the write transaction so that
+	// D1 batch mode does not hit query-after-write.
+	type viewDef struct {
+		Name string `db:"name"`
+		SQL  string `db:"sql"`
+	}
+	views := []viewDef{}
+	if err := app.DB().Select("name", "sql").
+		From("sqlite_master").
+		AndWhere(dbx.NewExp("sql is not null")).
+		AndWhere(dbx.HashExp{"type": "view"}).
+		All(&views); err != nil {
+		return err
+	}
+
 	return app.RunInTransaction(func(txApp App) error {
 		for _, newField := range newCollection.Fields {
 			// allow to continue even if there is no old field for the cases
@@ -192,21 +207,8 @@ func normalizeSingleVsMultipleFieldChanges(app App, newCollection *Collection, o
 
 			// temporary drop all views to prevent reference errors during the columns renaming
 			// (this is used as an "alternative" to the writable_schema PRAGMA)
-			views := []struct {
-				Name string `db:"name"`
-				SQL  string `db:"sql"`
-			}{}
-			err := txApp.DB().Select("name", "sql").
-				From("sqlite_master").
-				AndWhere(dbx.NewExp("sql is not null")).
-				AndWhere(dbx.HashExp{"type": "view"}).
-				All(&views)
-			if err != nil {
-				return err
-			}
 			for _, view := range views {
-				err = txApp.DeleteView(view.Name)
-				if err != nil {
+				if err := txApp.DeleteView(view.Name); err != nil {
 					return err
 				}
 			}
@@ -214,6 +216,8 @@ func normalizeSingleVsMultipleFieldChanges(app App, newCollection *Collection, o
 			originalName := newField.GetName()
 			oldTempName := "_" + newField.GetName() + security.PseudorandomString(5)
 
+			var err error
+
 			// rename temporary the original column to something else to allow inserting a new one in its place
 			_, err = txApp.DB().RenameColumn(newCollection.Name, originalName, oldTempName).Execute()
 			if err != nil {
diff --git a/core/collection_validate.go b/core/collection_validate.go
index 5b83bb8..6fc430a 100644
--- a/core/collection_validate.go
+++ b/core/collection_validate.go
@@ -75,7 +75,11 @@ func (validator *collectionValidator) run() error {
 
 	// generate fields from the query (overwriting any explicit user defined fields)
 	if validator.new.IsView() {
-		validator.new.Fields, _ = validator.app.CreateViewFields(validator.new.ViewQuery)
+		viewApp := validator.app
+		if info := validator.app.TxInfo(); info != nil && info.parent != nil {
+			viewApp = info.parent
+		}
+		validator.new.Fields, _ = viewApp.CreateViewFields(validator.new.ViewQuery)
 	}
 
 	// validate base fields
diff --git a/core/record_model.go b/core/record_model.go
index 3e472ff..863ce3d 100644
--- a/core/record_model.go
+++ b/core/record_model.go
@@ -48,6 +48,7 @@ type Record struct {
 	exportCustomData      bool
 	ignoreEmailVisibility bool
 	ignoreUnchangedFields bool
+	skipCascadeDelete     bool
 }
 
 const systemHookIdRecord = "__pbRecordSystemHook__"
@@ -1493,8 +1494,10 @@ func onRecordDeleteExecute(e *RecordEvent) error {
 		// queued for batch() so there is no in-transaction deadlock,
 		// and cascading first keeps all cascade queries before the
 		// main DELETE write, avoiding query-after-write.
-		if err := cascadeRecordDelete(txApp, e.Record, refs); err != nil {
-			return err
+		if !e.Record.skipCascadeDelete {
+			if err := cascadeRecordDelete(txApp, e.Record, refs); err != nil {
+				return err
+			}
 		}
 
 		return e.Next()
@@ -1504,12 +1507,56 @@ func onRecordDeleteExecute(e *RecordEvent) error {
 	return txErr
 }
 
-// cascadeRecordDelete triggers cascade deletion for the provided references.
+// cascadeRecordDelete triggers cascade deletion for the provided references
+// using recursive BFS. Phase 1 traverses the entire cascade graph (all
+// depths) to collect every record that must be deleted, setting
+// skipCascadeDelete=true on each to prevent re-entrant cascade. Phase 2
+// performs all deletions after every query is complete, avoiding
+// query-after-write inside the D1 batch transaction.
 //
 // NB! This method is expected to be called from inside of a transaction.
 func cascadeRecordDelete(app App, mainRecord *Record, refs map[*Collection][]Field) error {
-	// Sort the refs keys to ensure that the cascade events firing order is always the same.
-	// This is not necessary for the operation to function correctly but it helps having deterministic output during testing.
+	// Helper to collect records in refCollection that reference a given record.
+	collectRefRecords := func(targetRecord *Record, refCollection *Collection, field Field) ([]*Record, error) {
+		recordTableName := inflector.Columnify(refCollection.Name)
+		prefixedFieldName := recordTableName + "." + inflector.Columnify(field.GetName())
+
+		query := app.RecordQuery(refCollection)
+
+		if opt, ok := field.(MultiValuer); !ok || !opt.IsMultiple() {
+			query.AndWhere(dbx.HashExp{prefixedFieldName: targetRecord.Id})
+		} else {
+			query.AndWhere(dbx.Exists(dbx.NewExp(fmt.Sprintf(
+				`SELECT 1 FROM %s {{__je__}} WHERE [[__je__.value]]={:jevalue}`,
+				dbutils.JSONEach(prefixedFieldName),
+			), dbx.Params{
+				"jevalue": targetRecord.Id,
+			})))
+		}
+
+		if refCollection.Id == targetRecord.Collection().Id {
+			query.AndWhere(dbx.Not(dbx.HashExp{recordTableName + ".id": targetRecord.Id}))
+		}
+
+		batchSize := 4000
+		var collected []*Record
+		for {
+			batch := make([]*Record, 0, batchSize)
+			if err := query.Limit(int64(batchSize)).All(&batch); err != nil {
+				return nil, err
+			}
+			if len(batch) == 0 {
+				break
+			}
+			collected = append(collected, batch...)
+			if len(batch) < batchSize {
+				break
+			}
+		}
+		return collected, nil
+	}
+
+	// Sort the refs keys to ensure deterministic order.
 	sortedRefKeys := make([]*Collection, 0, len(refs))
 	for k := range refs {
 		sortedRefKeys = append(sortedRefKeys, k)
@@ -1518,67 +1565,89 @@ func cascadeRecordDelete(app App, mainRecord *Record, refs map[*Collection][]Fie
 		return sortedRefKeys[i].Name < sortedRefKeys[j].Name
 	})
 
-	// Phase 1: collect all related records across ALL collections
-	// and fields before queuing any writes. This avoids
-	// query-after-write inside the D1 batch transaction.
 	type cascadeBatch struct {
 		refRecords []*Record
 		field      Field
 	}
+
+	// Phase 1: BFS to collect ALL cascade targets at every depth.
 	var allBatches []cascadeBatch
+	visited := make(map[string]struct{})
+
+	type levelEntry struct {
+		records []*Record
+		field   Field
+	}
+
+	// Level 0: main record's direct cascade references
+	var currentLevel []levelEntry
 
 	for _, refCollection := range sortedRefKeys {
 		fields, ok := refs[refCollection]
-
 		if !ok || refCollection.IsView() {
-			continue // skip missing or view collections
+			continue
 		}
-
-		recordTableName := inflector.Columnify(refCollection.Name)
-
 		for _, field := range fields {
-			prefixedFieldName := recordTableName + "." + inflector.Columnify(field.GetName())
-
-			query := app.RecordQuery(refCollection)
-
-			if opt, ok := field.(MultiValuer); !ok || !opt.IsMultiple() {
-				query.AndWhere(dbx.HashExp{prefixedFieldName: mainRecord.Id})
-			} else {
-				query.AndWhere(dbx.Exists(dbx.NewExp(fmt.Sprintf(
-					`SELECT 1 FROM %s {{__je__}} WHERE [[__je__.value]]={:jevalue}`,
-					dbutils.JSONEach(prefixedFieldName),
-				), dbx.Params{
-					"jevalue": mainRecord.Id,
-				})))
+			relField, ok := field.(*RelationField)
+			if !ok || !relField.CascadeDelete {
+				continue
 			}
-
-			if refCollection.Id == mainRecord.Collection().Id {
-				query.AndWhere(dbx.Not(dbx.HashExp{recordTableName + ".id": mainRecord.Id}))
+			collected, err := collectRefRecords(mainRecord, refCollection, field)
+			if err != nil {
+				return err
 			}
+			if len(collected) > 0 {
+				currentLevel = append(currentLevel, levelEntry{collected, field})
+			}
+		}
+	}
+
+	// BFS through levels
+	for len(currentLevel) > 0 {
+		var nextLevel []levelEntry
+		for _, entry := range currentLevel {
+			for _, rec := range entry.records {
+				if _, seen := visited[rec.Id]; seen {
+					continue
+				}
+				visited[rec.Id] = struct{}{}
+				rec.skipCascadeDelete = true
+				allBatches = append(allBatches, cascadeBatch{[]*Record{rec}, entry.field})
 
-			batchSize := 4000
-			var collected []*Record
-			for {
-				batch := make([]*Record, 0, batchSize)
-				if err := query.Limit(int64(batchSize)).All(&batch); err != nil {
+				// Fetch NEXT level cascade references (BFS)
+				nextRefs, err := app.FindCachedCollectionReferences(rec.Collection())
+				if err != nil {
 					return err
 				}
-				if len(batch) == 0 {
-					break
+				nextRefKeys := make([]*Collection, 0, len(nextRefs))
+				for k := range nextRefs {
+					nextRefKeys = append(nextRefKeys, k)
 				}
-				collected = append(collected, batch...)
-				if len(batch) < batchSize {
-					break
+				sort.Slice(nextRefKeys, func(i, j int) bool {
+					return nextRefKeys[i].Name < nextRefKeys[j].Name
+				})
+				for _, refCollection := range nextRefKeys {
+					fields := nextRefs[refCollection]
+					for _, field := range fields {
+						relField, ok := field.(*RelationField)
+						if !ok || !relField.CascadeDelete {
+							continue
+						}
+						subCollected, err := collectRefRecords(rec, refCollection, field)
+						if err != nil {
+							return err
+						}
+						if len(subCollected) > 0 {
+							nextLevel = append(nextLevel, levelEntry{subCollected, field})
+						}
+					}
 				}
 			}
-
-			if len(collected) > 0 {
-				allBatches = append(allBatches, cascadeBatch{collected, field})
-			}
 		}
+		currentLevel = nextLevel
 	}
 
-	// Phase 2: process all writes after ALL queries are complete.
+	// Phase 2: delete all collected records
 	for _, batch := range allBatches {
 		if err := deleteRefRecords(app, mainRecord, batch.refRecords, batch.field); err != nil {
 			return err
diff --git a/core/view.go b/core/view.go
index 5b44e8b..bfefcc4 100644
--- a/core/view.go
+++ b/core/view.go
@@ -498,6 +498,23 @@ func findCollectionsByIdentifiers(app App, tables []identifier) (map[string]*Col
 		return nil, nil
 	}
 
+	// During an import the merged collection map is stored so that lookups
+	// resolve from the in-memory map instead of querying the database.
+	if m, ok := app.Store().GetOk("pocketflareImportCollections"); ok {
+		if imported, ok := m.(map[string]*Collection); ok {
+			result := make(map[string]*Collection, len(names))
+			for _, table := range tables {
+				for _, c := range imported {
+					if c.Name == table.original {
+						result[table.alias] = c
+						break
+					}
+				}
+			}
+			return result, nil
+		}
+	}
+
 	result := make(map[string]*Collection, len(names))
 	collections := make([]*Collection, 0, len(names))
 
diff --git a/ui/src/settings/backups/pocketflareRestore.js b/ui/src/settings/backups/pocketflareRestore.js
index bbf9936..80cab46 100644
--- a/ui/src/settings/backups/pocketflareRestore.js
+++ b/ui/src/settings/backups/pocketflareRestore.js
@@ -5,6 +5,8 @@
 const D1_MAX_BOUND_PARAMS = 100;
 const FILE_UPLOAD_CONCURRENCY = 3;
 
+function detectMime(e){var n=(e||"").split(".").pop()?.toLowerCase();return{jpg:"image/jpeg",jpeg:"image/jpeg",png:"image/png",gif:"image/gif",webp:"image/webp",svg:"image/svg+xml",pdf:"application/pdf",json:"application/json",html:"text/html",css:"text/css",js:"text/javascript"}[n]||"application/octet-stream"}
+
export function pocketflareRestorePage() {
    const data = store({
        phase: "idle",
@@ -21,6 +23,7 @@ export function pocketflareRestorePage() {
         activeRestore: null,
         progress: { done: 0, total: 0 },
         error: null,
+        cancelError: null,
         sessionId: null,
         fileUploadToken: null,
         statusMsg: "",
@@ -36,6 +38,9 @@ export function pocketflareRestorePage() {
            if (resp.activeRestore) {
                data.activeRestore = resp.activeRestore;
                data.fileUploadToken = resp.activeRestore.fileUploadToken;
+                data.sessionId = resp.activeRestore.sessionId;
+                data.phase = "resume";
+                return;
             }
             if (resp.empty) {
                 data.phase = "ready";
@@ -45,6 +50,29 @@ export function pocketflareRestorePage() {
             }
         } catch (err) {
             if (!err.isAbort) {
+                const t = sessionStorage.getItem("pocketflareRestoreToken");
+                if (t) {
+                    try {
+                        const resp = await fetch(app.pb.buildURL("/api/pocketflare/restore/session"), {
+                            headers: { "X-Pocketflare-Restore-Token": t },
+                        });
+                        if (resp.ok) {
+                            const body = await resp.json();
+                            data.activeRestore = {
+                                sessionId: body.sessionId,
+                                phase: body.phase,
+                                startedAt: body.startedAt,
+                                dbProgress: body.dbProgress,
+                                fileProgress: body.fileProgress,
+                                fileUploadToken: body.fileUploadToken,
+                            };
+                            data.fileUploadToken = body.fileUploadToken;
+                            data.sessionId = body.sessionId;
+                            data.phase = "resume";
+                            return;
+                        }
+                    } catch (_) { /* fall through to error */ }
+                }
                 data.error = err.response?.message || err.message || "Failed to check restore status";
                 data.phase = "error";
             }
@@ -96,6 +124,8 @@ export function pocketflareRestorePage() {
             fileUploadToken = resp.fileUploadToken;
             data.sessionId = sessionId;
             data.fileUploadToken = fileUploadToken;
+            sessionStorage.setItem("pocketflareRestoreToken", fileUploadToken);
+            sessionStorage.setItem("pocketflareRestoreSessionId", sessionId);
         } catch (err) {
             if (!err.isAbort) {
                 const info = err.response || {};
@@ -174,6 +204,124 @@ export function pocketflareRestorePage() {
 
         data.phase = "complete";
         data.statusMsg = "Restore complete. You may need to log in with restored superuser credentials.";
+        sessionStorage.removeItem("pocketflareRestoreToken");
+        sessionStorage.removeItem("pocketflareRestoreSessionId");
+    }
+
+    async function resumeRestore(file) {
+        if (!file || !file.name.endsWith(".zip")) {
+            data.error = "Please select a .zip backup file.";
+            data.phase = "error";
+            return;
+        }
+        data.phase = "reading";
+        data.statusMsg = "Reading backup zip for resume...";
+        let zip, sqlJS;
+        try {
+            const [JSZip, sqljsModule] = await Promise.all([import("jszip"), import("sql.js")]);
+            zip = await JSZip.default.loadAsync(file);
+            sqlJS = await sqljsModule.default({
+                locateFile: (f) => import.meta.env.BASE_URL + "assets/" + f,
+            });
+        } catch (err) {
+            data.error = "Failed to read backup zip: " + (err.message || err);
+            data.phase = "error";
+            return;
+        }
+        const dataDbEntry = zip.file("data.db");
+        if (!dataDbEntry) {
+            data.error = "Backup zip does not contain data.db.";
+            data.phase = "error";
+            return;
+        }
+        const auxDbEntry = zip.file("auxiliary.db");
+        const storageEntries = zip.file(/^storage\//);
+        const sid = data.sessionId || data.activeRestore?.sessionId || "";
+        const phase = data.activeRestore?.phase || "db";
+        if (phase !== "files") {
+            data.statusMsg = "Resuming database import...";
+            try {
+                await importDatabase(sqlJS, dataDbEntry, auxDbEntry, sid, (msg, done, total) => {
+                    data.statusMsg = msg;
+                    if (total > 0) {
+                        data.progress = { done, total };
+                        data.phase = "importing";
+                    }
+                });
+            } catch (err) {
+                data.error = "Database import failed: " + (err.message || err);
+                data.phase = "error";
+                return;
+            }
+            try {
+                await app.pb.send("/api/pocketflare/restore/phase", {
+                    method: "POST",
+                    body: JSON.stringify({ sessionId: sid, phase: "files" }),
+                    headers: restoreHeaders(),
+                });
+            } catch (err) {
+                if (!err.isAbort) {
+                    data.error = "Failed to transition to files phase: " + (err.response?.message || err.message || "");
+                    data.phase = "error";
+                    return;
+                }
+            }
+        }
+        if (storageEntries.length > 0) {
+            data.phase = "uploading";
+            data.totalFiles = storageEntries.length;
+            data.uploadedFiles = 0;
+            try {
+                await uploadStorageFiles(storageEntries, data.fileUploadToken, (done, total) => {
+                    data.uploadedFiles = done;
+                    data.totalFiles = total;
+                });
+            } catch (err) {
+                data.error = "File upload failed: " + (err.message || err);
+                data.phase = "error";
+                return;
+            }
+        }
+        data.phase = "finalizing";
+        data.statusMsg = "Finalizing restore...";
+        try {
+            await app.pb.send("/api/pocketflare/restore/finalize", {
+                method: "POST",
+                body: JSON.stringify({ sessionId: sid }),
+                headers: restoreHeaders(),
+            });
+        } catch (err) {
+            if (!err.isAbort) {
+                data.error = "Finalize failed: " + (err.response?.message || err.message || "");
+                data.phase = "error";
+                return;
+            }
+        }
+        data.phase = "complete";
+        data.statusMsg = "Restore complete. You may need to log in with restored superuser credentials.";
+        sessionStorage.removeItem("pocketflareRestoreToken");
+        sessionStorage.removeItem("pocketflareRestoreSessionId");
+    }
+
+    async function startOver() {
+        data.cancelError = null;
+        try {
+            const sid = data.sessionId || data.activeRestore?.sessionId || "";
+            const tok = data.fileUploadToken || sessionStorage.getItem("pocketflareRestoreToken") || "";
+            await app.pb.send("/api/pocketflare/restore/cancel", {
+                method: "POST",
+                body: JSON.stringify({ sessionId: sid }),
+                headers: tok
+                    ? { "X-Pocketflare-Restore-Token": tok, "Content-Type": "application/json" }
+                    : { "Content-Type": "application/json" },
+            });
+        } catch (err) {
+            if (!err.isAbort) {
+                data.cancelError =
+                    "Restore cannot be canceled after database import has started. Continue the restore, or start with a fresh Pocketflare target.";
+            }
+            return;
+        }
+        data.activeRestore = null;
+        data.sessionId = null;
+        data.fileUploadToken = null;
+        data.progress = { done: 0, total: 0 };
+        data.statusMsg = "";
+        data.error = null;
+        sessionStorage.removeItem("pocketflareRestoreToken");
+        sessionStorage.removeItem("pocketflareRestoreSessionId");
+        await checkStatus();
     }
 
     async function importDatabase(sqlJS, dataDbEntry, auxDbEntry, sessionId, onProgress) {
@@ -349,7 +497,7 @@ export function pocketflareRestorePage() {
         async function uploadOne(entry) {
             const blob = await entry.async("blob");
             const headers = { "X-Pocketflare-File-Key": entry.name, "X-Pocketflare-Restore-Token": fileUploadToken };
-            if (blob.type) headers["X-Pocketflare-File-Content-Type"] = blob.type;
+            headers["X-Pocketflare-File-Content-Type"] = detectMime(entry.name) || blob.type || "application/octet-stream";
             const resp = await fetch("/api/pocketflare/restore/files", { method: "PUT", headers, body: blob });
             if (!resp.ok) {
                 const err = await resp.json().catch(() => ({}));
@@ -371,6 +519,8 @@ export function pocketflareRestorePage() {
         data,
         handleFile,
         checkStatus,
+        resumeRestore,
+        startOver,
         render: function() {
             return t.div(
                 { className: "block" },
@@ -486,3 +636,46 @@ export function pocketflareRestorePage() {
                             t.button({
                                 type: "button",
                                 className: "btn secondary m-t-sm",
+                                onclick: () => checkStatus(),
+                                textContent: "Try again",
+                            }),
+                        )
+                        : null,
+                () =>
+                    data.phase === "resume"
+                        ? t.div(
+                            { "html-data-test": "restore-active-session" },
+                            t.p({ className: "m-b-sm txt-bold" }, "Active restore session detected"),
+                            t.p(
+                                { className: "m-b-sm" },
+                                "Session: " + (data.activeRestore?.sessionId || data.sessionId || ""),
+                            ),
+                            t.p(
+                                { className: "m-b-sm" },
+                                "Phase: " + (data.activeRestore?.phase || ""),
+                            ),
+                            t.p(
+                                { className: "m-b-sm" },
+                                "Started: " + (data.activeRestore?.startedAt || ""),
+                            ),
+                            () =>
+                                data.cancelError
+                                    ? t.div(
+                                        { "html-data-test": "restore-cancel-error", className: "alert danger m-b-sm" },
+                                        t.p(null, data.cancelError),
+                                    )
+                                    : null,
+                            t.input({
+                                type: "file",
+                                accept: ".zip",
+                                className: "m-b-sm",
+                                onchange: (e) => {
+                                    const f = e.target?.files?.[0];
+                                    if (f) resumeRestore(f);
+                                },
+                            }),
+                            t.button({
+                                type: "button",
+                                "html-data-test": "restore-start-over",
+                                className: "btn secondary m-t-sm",
+                                onclick: () => startOver(),
+                                textContent: "Start Over",
+                            }),
+                        )
+                        : null,
+            );
+        },
+    };
+}
