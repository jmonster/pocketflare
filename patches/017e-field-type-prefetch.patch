diff --git a/core/collection_record_table_sync.go b/core/collection_record_table_sync.go
index 3ce9255..b004504 100644
--- a/core/collection_record_table_sync.go
+++ b/core/collection_record_table_sync.go
@@ -32,6 +32,24 @@ func (app *BaseApp) SyncRecordTableSchema(newCollection *Collection, oldCollecti
 		}
 	}
 
+	// Pre-fetch view definitions outside the write transaction so that
+	// D1 batch mode does not hit query-after-write. When called from
+	// inside an outer transaction (e.g. onCollectionSaveExecute after
+	// e.Next() has queued a write), route the SELECT through the parent
+	// (non-transactional) app.
+	views := []viewDef{}
+	viewApp := app
+	if info := app.TxInfo(); info != nil && info.parent != nil {
+		viewApp = info.parent
+	}
+	if err := viewApp.DB().Select("name", "sql").
+		From("sqlite_master").
+		AndWhere(dbx.NewExp("sql is not null")).
+		AndWhere(dbx.HashExp{"type": "view"}).
+		All(&views); err != nil {
+		return err
+	}
+
 	txErr := app.RunInTransaction(func(txApp App) error {
 		// create
 		// -----------------------------------------------------------
@@ -137,7 +155,7 @@ func (app *BaseApp) SyncRecordTableSchema(newCollection *Collection, oldCollecti
 			}
 		}
 
-		if err := normalizeSingleVsMultipleFieldChanges(txApp, newCollection, oldCollection); err != nil {
+		if err := normalizeSingleVsMultipleFieldChanges(txApp, newCollection, oldCollection, views); err != nil {
 			return err
 		}
 
@@ -161,7 +179,12 @@ func (app *BaseApp) SyncRecordTableSchema(newCollection *Collection, oldCollecti
 	return nil
 }
 
-func normalizeSingleVsMultipleFieldChanges(app App, newCollection *Collection, oldCollection *Collection) error {
+type viewDef struct {
+	Name string `db:"name"`
+	SQL  string `db:"sql"`
+}
+
+func normalizeSingleVsMultipleFieldChanges(app App, newCollection *Collection, oldCollection *Collection, views []viewDef) error {
 	if newCollection.IsView() || oldCollection == nil {
 		return nil // view or not an update
 	}
@@ -192,21 +215,8 @@ func normalizeSingleVsMultipleFieldChanges(app App, newCollection *Collection, o
 
 			// temporary drop all views to prevent reference errors during the columns renaming
 			// (this is used as an "alternative" to the writable_schema PRAGMA)
-			views := []struct {
-				Name string `db:"name"`
-				SQL  string `db:"sql"`
-			}{}
-			err := txApp.DB().Select("name", "sql").
-				From("sqlite_master").
-				AndWhere(dbx.NewExp("sql is not null")).
-				AndWhere(dbx.HashExp{"type": "view"}).
-				All(&views)
-			if err != nil {
-				return err
-			}
 			for _, view := range views {
-				err = txApp.DeleteView(view.Name)
-				if err != nil {
+				if err := txApp.DeleteView(view.Name); err != nil {
 					return err
 				}
 			}
@@ -214,6 +224,8 @@ func normalizeSingleVsMultipleFieldChanges(app App, newCollection *Collection, o
 			originalName := newField.GetName()
 			oldTempName := "_" + newField.GetName() + security.PseudorandomString(5)
 
+			var err error
+
 			// rename temporary the original column to something else to allow inserting a new one in its place
 			_, err = txApp.DB().RenameColumn(newCollection.Name, originalName, oldTempName).Execute()
 			if err != nil {
